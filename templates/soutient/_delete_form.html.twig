<form method="post" action="{{ path('app_soutien_delete', {'id': soutien.id}) }}" 
      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce soutien ?');"
      class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soutien.id) }}">
    <button class="btn btn-outline-danger btn-sm" title="Supprimer">
        🗑️
    </button>
</form>